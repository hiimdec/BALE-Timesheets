# BALE-Timesheets
Timesheets

## Postcode distance dataset

`postcode-distances.json` maps UK outcode districts to driving-distance estimates (miles) from W1F 9SE and an inside-M25 boolean. It is committed to the repo for offline use.

To regenerate: `node scripts/build-postcode-distances.js`
